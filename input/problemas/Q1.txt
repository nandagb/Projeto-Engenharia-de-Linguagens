real func expressao(real a, real b, int z){
    retorne a * a - b + real(z);
}

vazio func main(){
   real x = 3.5;
   real y = 2.1;
   int c = 4;

   real resultado = expressao(x, y, c);
   saida("resultado da expressão: " + texto(resultado) + "\n");
}
