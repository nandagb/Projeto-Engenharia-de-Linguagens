vazio func main() {
   int c1 = 0;
   int c2 = 0;
   int c3 = 0;
   int c4 = 0;
   
   int num = 0;

   saida("Para sair do programa, digite um valor negativo: \n");
   saida("Digite um número: \n");

   entrada(num);

   enquanto (num >= 0) {

      saida("digite outro número: \n");
      
      # Verifica os intervalos
      se (num <= 25) {
         c1++;
      } 
      senao se (num <= 50) {
         c2++;
      } 
      senao se (num <= 75) {
         c3++;
      } 
      senao se (num <= 100) {
         c4++;
      }

      entrada(num);
   }

   saida("quantidade de números no primeiro intervalo: " + texto(c1) + "\n");
   saida("quantidade de números no segundo intervalo: " + texto(c2) + "\n");
   # ta dando erro na tabela de simbolos ao recuperar o tipo de c3 por alguma razão...
   saida("quantidade de números no terceiro intervalo: " + texto(c3) + "\n");
   saida("quantidade de números no quarto intervalo: " + texto(c4) + "\n");
}