Registro No = {
    int chave,
    int esquerda,
    int direita,
    int nivel
};

vazio func main() {
    Lista<No> arvore = novo Lista<>()(7);
    int raiz = 0;
    int proximoIndice = 7;

    # Montando a árvore binária
    arvore[0].chave = 50;
    arvore[0].esquerda = 1;
    arvore[0].direita = 2;
    arvore[0].nivel = 0;

    arvore[1].chave = 30;
    arvore[1].esquerda = 3;
    arvore[1].direita = 4;
    arvore[1].nivel = 1;

    arvore[2].chave = 70;
    arvore[2].esquerda = 5;
    arvore[2].direita = 6;
    arvore[2].nivel = 1;

    arvore[3].chave = 20;
    arvore[3].esquerda = -1;
    arvore[3].direita = -1;
    arvore[3].nivel = 2;

    arvore[4].chave = 40;
    arvore[4].esquerda = -1;
    arvore[4].direita = -1;
    arvore[4].nivel = 2;

    arvore[5].chave = 60;
    arvore[5].esquerda = -1;
    arvore[5].direita = -1;
    arvore[5].nivel = 2;

    arvore[6].chave = 80;
    arvore[6].esquerda = -1;
    arvore[6].direita = -1;
    arvore[6].nivel = 2;

    # Encontrar máximo (indo todo pra direita)
    int indiceMax = raiz;
    enquanto (arvore[indiceMax].direita != -1) {
        indiceMax = arvore[indiceMax].direita;
    }

    # Encontrar mínimo (indo todo pra esquerda)
    int indiceMin = raiz;
    enquanto (arvore[indiceMin].esquerda != -1) {
        indiceMin = arvore[indiceMin].esquerda;
    }

    int chaveMax = arvore[indiceMax].chave;
    int nivelMax = arvore[indiceMax].nivel;
    int chaveMin = arvore[indiceMin].chave;
    int nivelMin = arvore[indiceMin].nivel;

    saida("Minimo: ");
    saida(texto(chaveMin));
    saida(" no nivel ");
    saida(texto(nivelMin));
    saida("\n");

    saida("Maximo: ");
    saida(texto(chaveMax));
    saida(" no nivel ");
    saida(texto(nivelMax));
    saida("\n");
}
