Registro No = {
    int chave,
    int esquerda,
    int direita,
    int nivel
};

vazio func main() {
    Lista<No> arvore = novo Lista<>()(3);

    arvore[0].nivel = 0;

    int x = arvore[0].nivel + 1;
}
