Registro No = {
    int chave,
    int esquerda,
    int direita,
    int nivel
};

vazio func main() {
    Lista<No> arvore = novo Lista<>()(3);

    # só pra garantir que o campo existe no C gerado
    arvore[0].nivel = 0;

    # aqui é o nosso teste específico:
    int x = arvore[0].nivel + 1;
}
